# Model artifacts

- `active/` contains the five compact checkpoints referenced by the reviewed
  FNN/CNN workflows and identified by hashes in `docs/neural_network_review.md`.
- `legacy/` contains the former `networks/` checkpoint family and historical
  training figures.

Overlapping CNN numbers across these folders are not interchangeable. Exact
training manifests and seeds are unknown.
